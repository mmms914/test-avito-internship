server:
  port:
  read_timeout: 10s
  write_timeout: 10s
  graceful_timeout: 30s

db:
  host:
  port:
  user:
  password:
  name:
  ssl_mode:
  connect_timeout: 5s
  max_open_conns: 25
  max_idle_conns: 10
  max_conn_lifetime: 5m

auth:
  jwt_secret:
  token_duration_hours:
